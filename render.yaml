services:
  - type: web
    name: backend-api
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PUPPETEER_SKIP_DOWNLOAD
        value: "false"
    preDeployCommand: bash scripts/install-chromium.sh
